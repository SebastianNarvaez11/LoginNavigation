package com.sebastiannarvaez.loginnavigationapp.core.presentation.theme

import androidx.compose.ui.graphics.Color


// --- Brand Colors (Extracted from Design) ---
val NeonLime = Color(0xFFD2F806)       // El color principal vibrante (Tarjetas, Focus)
val NeonLimeDark = Color(0xFFA8C800)   // Variación para bordes o estados presionados
val ElectricBlue = Color(0xFF2B80FF)   // El azul del botón "Add money" y fondo de Wallet
val DeepBlue = Color(0xFF0055CC)       // Variación oscura del azul

// --- Backgrounds & Surfaces ---
val BlackBackground = Color(0xFF101010) // Fondo casi negro absoluto de la app
val DarkSurface = Color(0xFF1C1C1E)     // El gris oscuro de las tarjetas/listas
val LightSurface = Color(0xFF2C2C2E)    // Un gris un poco más claro (ej. Search bar)

// --- Functional Colors ---
val White = Color(0xFFFFFFFF)
val White80 = Color(0xCCFFFFFF)         // Texto secundario
val Black = Color(0xFF000000)
val ErrorRed = Color(0xFFFF4D4D)        // Un rojo estándar para errores (basado en el icono de Netflix)

// --- Legacy / Fallbacks (Si los necesitas para algo más) ---
val Purple80 = Color(0xFFD0BCFF)
val PurpleGrey80 = Color(0xFFCCC2DC)
val Pink80 = Color(0xFFEFB8C8)